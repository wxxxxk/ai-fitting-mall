# 이 파일을 복사해서 .env 로 만들고 실제 값을 채운다.
#   cp .env.example .env
# .env 는 절대 커밋하지 않는다 (.gitignore 등록됨).

# fal.ai API 키 (https://fal.ai 에서 발급)
FAL_KEY=your-falai-key-here
# 서버 포트
PORT=8000

# OpenAI API 키 (https://platform.openai.com 에서 발급)
OPENAI_API_KEY=your-openai-key-here
